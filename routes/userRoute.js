const router = require('express').Router();
const userController = require('../Controllers/userController');

// router.route('/').get(userController.getUsers).post(userController)

module.exports = router;
